version: cibuild-{build}
image: Ubuntu2004
stack: jdk 16
build_script:
  - java -version
  - chmod +x gradlew
  - ./gradlew buildDependents --no-daemon
artifacts:
- path: build\libs
test: off
deploy: off
